server {
    listen       80;
    server_name  localhost;

    types {}
        default_type text/html;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        autoindex on;
 
    }
}
